export const has = (elem, className) => elem.classList.contains(className);
export const add = (elem, className) => elem.classList.add(className);
export const remove = (elem, className) => elem.classList.remove(className);
