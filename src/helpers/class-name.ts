export const has = (elem: HTMLElement, className: string) => elem.classList.contains(className);
export const add = (elem: HTMLElement, className: string) => elem.classList.add(className);
export const remove = (elem: HTMLElement, className: string) => elem.classList.remove(className);
